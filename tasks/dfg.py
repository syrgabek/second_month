# for hero in heroes:
            if hero.health > 0:
                self.__critic_damage = self.damage + int(boss.damage / len(heroes) / 2)
                self.damage = self.__critic_damage

self.__critic_damage = self.damage + boss.damage
self.damage = self.__critic_damage


Thor.damage = True
        if self.health > 0:
            if self.damage == True:
                boss.damage = 0
                if boss.damage == 0:
                    self.damage = 0
            else:
                boss.damage = 50

if self.health > 0:
    if boss.health - self.damage:
        boss.damage = 0
if boss.health - self.damage:
    boss.damage = 50


golem
if self.health > 0:
    for hero in heroes:
        if self.health - boss.damage:
            self.__protect = boss.damage - (boss.damage - 40)
            hero.health = hero.health - boss.damage + self.__protect



            for hero in heroes:
                if hero.health > 0:
                    if boss.damage == 50:
                        self.__protect = int(boss.damage / 5)
                        hero.health = hero.health + self.__protect
                    else:
                        hero.health = hero.health - boss.damage
